services:
  - type: web
    name: team-task-system
    env: java
    rootDir: backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 11
      - key: PORT
        value: 8080
    plan: free
